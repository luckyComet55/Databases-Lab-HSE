#include <fstream>
#include <vector>
#include <string>

#include "Types/Meta/Constants.hpp"
#include "EntityManager.hpp"

namespace db {

    namespace fs {

        template <typename T>
        db::meta::TransactionStatus EntityManager<T>::insertRecord(const std::vector<std::string>& values) {
            
        }
    }
}